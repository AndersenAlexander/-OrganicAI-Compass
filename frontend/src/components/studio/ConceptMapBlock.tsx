export function ConceptMapBlock({ title }: { title: string }) {
  return <div className="rounded-2xl border border-teal/20 bg-white/85 p-5 text-sm font-semibold text-navy shadow-sm">{title}</div>;
}
