import {useState} from "react";import {saveRagFeedback,saveSourceFeedback,type RagFeedbackRequest} from "../../api/ragApi";
const answerRatings=[["Helpful","helpful"],["Partially","partially_helpful"],["Not helpful","not_helpful"]] as const;const sourceRatings=[["Relevant","relevant"],["Partially relevant","partially_relevant"],["Not relevant","not_relevant"]] as const;
export function RagFeedback({runId,sourceId}:{runId:string;sourceId?:string}){const [selected,setSelected]=useState("");const [reason,setReason]=useState("");const [status,setStatus]=useState("");const ratings=sourceId?sourceRatings:answerRatings;async function submit(rating:string){setSelected(rating);setStatus("Saving feedback…");try{const payload={rating,reason_code:reason||undefined} as RagFeedbackRequest;if(sourceId)await saveSourceFeedback(runId,sourceId,payload);else await saveRagFeedback(runId,payload);setStatus("Feedback saved for research evaluation.")}catch{setStatus("Your feedback could not be saved. Please try again.")}}return <div className="rag-feedback"><p>{sourceId?"Was this source relevant?":"Was this response useful?"}</p><div>{ratings.map(([label,value])=><button type="button" key={value} aria-pressed={selected===value} onClick={()=>void submit(value)}>{label}</button>)}</div>{!sourceId&&(selected==="partially_helpful"||selected==="not_helpful")&&<label>What was missing?<select value={reason} onChange={e=>setReason(e.target.value)}><option value="">Choose a reason</option><option value="too_general">Too general</option><option value="unsupported_claim">Not grounded enough</option><option value="contradicts_profile">Not relevant to my profile</option><option value="not_actionable">Not actionable</option><option value="unclear">Unclear</option><option value="other">Other</option></select></label>}<span role="status" aria-live="polite">{status}</span></div>}
