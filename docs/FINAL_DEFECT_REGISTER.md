# Final Defect Register

| ID | Severity | Status | Finding | Evidence / safe resolution |
|---|---|---|---|---|
| RC-001 | Release blocker | Open | The configured PostgreSQL port is a Docker Desktop proxy, not a native PostgreSQL listener: PID 15464 is `com.docker.backend.exe`. Docker Desktop's API proxy repeatedly logs `no route to host` for its Linux VM endpoint `192.168.65.7:2376`; Docker requests and the PostgreSQL client time out, while container health checks time out. | In an authorized desktop session, restore Docker Desktop's WSL/VM network backend (start/restart Docker Desktop Service/Desktop; do not reset Docker or remove volumes), wait for the existing PostgreSQL container health check to pass, then run read-only `SELECT 1`, `alembic current`, health/readiness, existing-account, fresh-QA-account, and non-sensitive count checks. |
| RC-002 | Medium | Open | The workstation's repository `.venv` Python launcher is not portable/usable; isolated validation used the installed Python interpreter explicitly. | Recreate or repair the development virtual environment using the approved dependency lock/process, then document the reproducible command. Do not change runtime database data. |
| RC-003 | Medium | Open | Frontend production build warns that generated chunks exceed the configured 500 kB warning threshold. | Perform a measured post-release chunking/performance pass; no risky late-stage refactor was made in this audit. |
| RC-004 | Low | Open | Backend suite reports third-party `python-jose` `datetime.utcnow` deprecation warnings. | Upgrade or replace dependency in a planned maintenance change with regression coverage. |
| RC-005 | Low | Open | Windows Playwright managed web-server teardown can linger after assertions/tests complete. | Improve deterministic test-server cleanup in a dedicated test-infrastructure change; do not treat it as a product assertion failure. |
| RC-006 | High | Resolved | Demo seeding attempted adaptive recommendations before deriving active hypotheses, causing the demo-account regression. | `demo_seed_service.py` now derives hypotheses from already-seeded career matches before creating originality demo records; targeted demo suite passed (9). |
| RC-007 | Low | Resolved | Human-diagnostic E2E used stale required-field/CTA selectors relative to the current validated UI. | Test-only selectors were aligned with the current required fields and `Test this direction` CTA; no product behavior was changed. |
