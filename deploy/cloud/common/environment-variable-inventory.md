# Environment Variable Inventory

Use `.env.cloud-staging.example` as the provider-neutral template. Values must be supplied through managed secrets or protected environment configuration, not committed files.
